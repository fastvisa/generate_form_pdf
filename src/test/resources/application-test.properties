# Test Configuration
# These properties are used during testing to prevent bean creation failures

# Mock AWS Configuration (not used in actual tests)
aws.accessKey=mock-access-key
aws.secretKey=mock-secret-key
aws.s3bucket.name=mock-bucket
aws.s3bucket.region=us-east-1

# Disable logging during tests
logging.level.org.springframework=WARN
logging.level.com.fastvisa=WARN